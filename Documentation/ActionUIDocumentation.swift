import Foundation

/// Resource bundle containing ActionUI documentation:
/// schemas (.md), element templates (.json), and the element index.
///
/// Access at runtime via `ActionUIDocumentation.bundle`.
public enum ActionUIDocumentation {
    public static let bundle = Bundle.module
}
